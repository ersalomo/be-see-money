import { LinkImageInterceptor } from './link-image.interceptor';

describe('LinkImageInterceptor', () => {
  it('should be defined', () => {
    expect(new LinkImageInterceptor()).toBeDefined();
  });
});
